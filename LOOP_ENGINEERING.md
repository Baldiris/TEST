# ループエンジニアリング記録

## Design Competition ROUND 2 — 構造から再設計（2026-09-20）

### 問題点

- ROUND 1は共通のカード配置に配色、角丸、影を着せ替えただけで、操作体験の比較になっていなかった。
- 英字見出し、ピル型ラベル、グラデーション、均一なカード構成が多く、既視感のあるAI生成風デザインだった。

### 改善内容

- A「駅サイン式」：路線、進行方向、操作の順に読む直線的な案内画面へ変更。
- B「盤面主役式」：路線盤面を中心に置き、候補番号とサイコロを盤面上へ直接配置。
- C「旅程手帳式」：切符、時系列ログ、スタンプ、候補券で旅の記録を辿る構成へ変更。
- 3案でHTML構造を共有せず、それぞれ専用の情報設計とCSSを持たせた。
- 装飾目的のグラデーション、グロー、共通カードUI、星評価表を撤去した。

### 判定基準

- 色の好みではなく、「駅で迷わず遊び続けられるか」で比較する。

## Design Competition 01 — ビジュアル3案（2026-09-20）

### 目的

- ゲームルールやV0.5本編を変更せず、次期デザインの方向性だけを公平に比較する。

### 候補

- A「メトロ・サイン計画」：視認性、案内性、現行からの移行しやすさを重視。
- B「トラベルボードゲーム」：桃鉄らしい楽しさ、触りたくなる演出を重視。
- C「TOKYO NIGHT LINE」：路線カラー、夜の東京、没入感を重視。

### 実装内容

- 同じ駅、同じルート、同じサイコロ結果を3テーマで切り替え比較できる専用ページを追加。
- スマホ用の固定テーマ切替、LocalStorageのみを使った投票、評価表を追加。
- 本編への影響を避けるため、既存の `index.html`、`app.js`、`style.css` は変更していない。

### 次の工程

- 選ばれた案をベースに、良かった要素を他案から1〜2個だけ取り込んだ実装仕様を決める。

## V0.5 — 候補選択とルート把握（2026-09-20）

### 問題点

- 盤面が表示幅 682px に対して 2200pxあり、候補駅を横スクロールだけで探す負担が大きかった。
- サイコロ後に候補数しか表示されず、駅名やゴールへの近さを比較できなかった。
- 乗換接続線が通常線と重なり、乗換駅と最短ルート上の乗換を判別しにくかった。
- スマホではサイコロが盤面より下にあり、毎ターンの操作距離が長かった。
- 出目確定後にもサイコロを振り直せた。

### 改善内容

- サイコロを盤面より上へ移動し、候補駅カードを追加した。
- 候補を「ゴールへの前進量」「ゴールまでの残り駅数」で比較できるようにした。
- 最短ルートを駅カード形式で表示するルートガイドを追加した。
- 盤面幅を 2200px から 1840pxへ縮小し、現在地を初期フォーカスにした。
- 乗換駅を二重丸にし、最短ルート上の乗換接続を濃く表示した。
- SVG駅ノードのタップ範囲を拡大し、モバイル用ボタンを44px以上にした。
- 到着時の電車アニメーションとゴール時の演出を追加した。
- V0.4のLocalStorageデータをV0.5へ移行できるようにした。
- 出目確定後の振り直しを禁止した。

### テスト結果

- `node --check app.js`: 成功
- 138駅の連結性、双方向エッジ、乗換0マス、最短距離一致候補: 成功
- V0.4保存データ移行、開始→候補選択→到着→クエスト→ゴールのスモークテスト: 成功
- HTML要素参照、V0.5表記、レスポンシブ要件のUI契約テスト: 成功

### 次の改善候補

- 乗換接続線が密集する都心部のレイアウトを、路線単位ではなくエリア単位で再配置する。
- 全体表示でも駅名が読める「主要駅表示」と、詳細表示の切替を追加する。
- 候補駅を盤面上でも順位やゴール方向で識別しやすくする。
